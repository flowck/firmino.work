name: Deploy to Surge

on:
  push:
      branches: [main]

jobs:
  my-job:
    name: My job
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to changani.me
        uses: dswistowski/surge-sh-action@v1
        with:
          domain: 'changani.me'
          project: '.'
          login: ${{ secrets.SURGE_LOGIN }}
          token: ${{ secrets.SURGE_TOKEN }}